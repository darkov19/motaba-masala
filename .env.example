# App mode: dev|development|local|test|production
MASALA_APP_ENV=development

# Required in non-dev. In dev it is still recommended to set explicitly.
MASALA_JWT_SECRET=replace-with-long-random-secret

# Used only when user count is zero (bootstrap).
MASALA_BOOTSTRAP_ADMIN_USERNAME=admin
MASALA_BOOTSTRAP_ADMIN_PASSWORD=replace-with-strong-password-at-least-16-chars
